{
  "name": "netcert-quiz",
  "version": "1.0.0",
  "description": "Taiwan Network Certification Quiz App with Auth, XP, and Progress Tracking",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/ && cp -r css js data public/"
  },
  "dependencies": {},
  "devDependencies": {
    "pdf-parse": "^2.4.5",
    "pdfjs-dist": "^6.0.227",
    "xlsx": "^0.18.5"
  }
}
